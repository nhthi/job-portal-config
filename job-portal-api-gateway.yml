server:
  port: 5000

eureka:
  client:
    fetch-registry: true
    registry-with-eurek: true
    service-url:
      defaultZone: http://localhost:8761/eureka/

  instance:
    prefer-ip-address: true